var popUp = '';

function users(action, id) {
    if(typeof(id) === "undefined") id = 0;

    switch (action) {
        case 'formNew':
            
            break;
        case 'insert':
            
            break;
        case 'update':
            
            break;
        case 'delete':
            
            break;
        case 'validateRegister':
            
            break;
        default:
            alert('Opción inválida.');
            break;
    }
}